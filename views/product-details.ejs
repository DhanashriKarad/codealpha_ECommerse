<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %> - E-Commerce Site</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <nav>
            <div class="logo">
                <h1>E-Commerce</h1>
            </div>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/products">Products</a></li>
                <% if (user) { %>
                    <li><a href="/cart">Cart</a></li>
                    <li><a href="/orders">Orders</a></li>
                    <li><a href="/profile">Profile</a></li>
                    <% if (user.role === 'admin') { %>
                        <li><a href="/admin">Admin</a></li>
                    <% } %>
                    <li><a href="/auth/logout">Logout</a></li>
                <% } else { %>
                    <li><a href="/auth/login">Login</a></li>
                    <li><a href="/auth/register">Register</a></li>
                <% } %>
            </ul>
        </nav>
    </header>

    <main>
        <div class="product-details">
            <div class="product-image">
                <a href="https://www.amazon.com/s?k=<%= encodeURIComponent(product.name.replace(/\s+/g, '+')) %>" target="_blank" class="amazon-link">
                    🔍 View similar <%= product.name %> on Amazon
                </a>
            </div>
            <div class="product-info">
                <h2><%= product.name %></h2>
                <p><%= product.description %></p>
                <p class="price">$<%= product.price %></p>
                <p>Stock: <%= product.stock %></p>
                <p>Category: <%= product.category_name %></p>
                
                <% if (user) { %>
                    <form action="/cart/add/<%= product.id %>" method="POST">
                        <label for="quantity">Quantity:</label>
                        <input type="number" id="quantity" name="quantity" value="1" min="1" max="<%= product.stock %>">
                        <button type="submit" class="btn">Add to Cart</button>
                    </form>
                <% } else { %>
                    <p>Please <a href="/auth/login">login</a> to add to cart.</p>
                <% } %>
            </div>
        </div>

        <div class="reviews">
            <h3>Reviews</h3>
            <% if (reviews.length > 0) { %>
                <% reviews.forEach(review => { %>
                    <div class="review">
                        <p><strong><%= review.user_name %></strong> - <%= review.rating %>/5</p>
                        <p><%= review.comment %></p>
                        <p><%= new Date(review.created_at).toLocaleDateString() %></p>
                    </div>
                <% }); %>
            <% } else { %>
                <p>No reviews yet.</p>
            <% } %>
            
            <% if (user) { %>
                <form action="/products/<%= product.id %>/review" method="POST">
                    <h4>Add Review</h4>
                    <label for="rating">Rating:</label>
                    <select id="rating" name="rating" required>
                        <option value="5">5 stars</option>
                        <option value="4">4 stars</option>
                        <option value="3">3 stars</option>
                        <option value="2">2 stars</option>
                        <option value="1">1 star</option>
                    </select>
                    <textarea name="comment" placeholder="Your review..." required></textarea>
                    <button type="submit" class="btn">Submit Review</button>
                </form>
            <% } %>
        </div>
    </main>

    <footer>
        <p>&copy; 2023 E-Commerce Site. All rights reserved.</p>
    </footer>

    <script src="/js/script.js"></script>
</body>
</html>
